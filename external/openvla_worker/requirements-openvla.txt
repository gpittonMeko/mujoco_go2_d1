# Dipendenze Hugging Face per inferenza OpenVLA (installa **dopo** torch CUDA sul PC RTX).
# Uso: pip install -r requirements-openvla.txt
# Oppure: bootstrap_worker_host.ps1 -InstallOpenvlaHF
transformers>=4.44.0,<5
accelerate>=0.33.0
safetensors>=0.4.0
pillow>=10.0.0
sentencepiece>=0.1.99
timm>=1.0.0,<2
einops>=0.7.0
