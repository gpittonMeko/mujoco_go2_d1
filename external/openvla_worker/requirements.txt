# Server HTTP + modalità ``planner`` (box_grasp_planner dal repo).
# OpenVLA HF: vedi ``requirements-openvla.txt`` + variabili ``OPENVLA_USE_HF`` / README.
flask>=3.0.0,<4
gunicorn>=22.0.0,<24
numpy>=1.23,<3
opencv-python-headless>=4.8,<5
