# H2 demo — Jetson Thor

Demo in `~/h2_demo`. SSH: `unitree` / `123`.

**Prima della demo:** Regular Mode 1 sul telecomando + servizio mani attivo sul `.162`.

```powershell
# Dal PC lab
python scripts/h2_start_brainco_pc2.py
python scripts/h2_smoke_remote.py demo --confirm-arm
```
